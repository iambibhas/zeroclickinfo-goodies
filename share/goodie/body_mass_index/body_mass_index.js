DDH.body_mass_index = DDH.body_mass_index || {};

DDH.body_mass_index.build = function(ops) {

    console.log(ops);

    // define Handlebars helpers and private functions here

    // check if inputs exist (dig into ops object)
    // and calculate the result

    // otherwise show a blank form and wait for input

    return {
         onShow: function() {
            console.log("Look here!");
         }
    };
};